// 1 5 3 5 2 3 2
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int k = 0; k < n; k++)
    {
        cin >> arr[k];
    }
    const int N = (SIZE_MAX / sizeof(int)) + 1;
    int idx[N];
    for (int l = 0; l < N; l++)
    {
        idx[l] = -1;
    }
    int minidx = INT_MAX;
    return 0;
}